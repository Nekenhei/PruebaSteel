import { FilterGeneroPipe } from './filter-genero.pipe';

describe('FilterGeneroPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGeneroPipe();
    expect(pipe).toBeTruthy();
  });
});
