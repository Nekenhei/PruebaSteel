import { FilterAutorPipe } from './filter-autor.pipe';

describe('FilterAutorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAutorPipe();
    expect(pipe).toBeTruthy();
  });
});
