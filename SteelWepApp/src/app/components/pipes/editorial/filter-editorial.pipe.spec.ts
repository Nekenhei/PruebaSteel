import { FilterEditorialPipe } from './filter-editorial.pipe';

describe('FilterEditorialPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEditorialPipe();
    expect(pipe).toBeTruthy();
  });
});
