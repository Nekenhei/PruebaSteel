import { FilterTituloPipe } from './filter-titulo.pipe';

describe('FilterTituloPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTituloPipe();
    expect(pipe).toBeTruthy();
  });
});
